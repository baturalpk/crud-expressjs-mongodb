
/**
 * DEPRECATED for the time being..!
*/

/*
const update_name = document.querySelector('#change-name-button')

update_name.addEventListener('click', _ => {
    fetch('crud', {
        method: 'put',
        headers: {'Content-Type': 'application/json'}, 
        body: JSON.stringify({
            name: "TEXT",
            surname: "TEXT2"            
        }) 
    })
    console.log("work-test");
})

const update_surname = document.querySelector('#change-surname-button')

update_surname.addEventListener('click', _ => {
    fetch('crud', {
        method: 'put',
        headers: {'Content-Type': 'application/json'}
    })
})
*/